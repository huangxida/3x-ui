name: Docker Workflow Placeholder

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current publish flow
        run: |
          echo "DockerHub publishing has been retired."
          echo "Formal tag releases now use deploy.yml to publish GHCR images and prune old image versions."
